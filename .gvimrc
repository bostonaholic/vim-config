" use gui tabs
  set guioptions+=e

" kill the toolbar
  set guioptions-=T

" kill the menubar
  set guioptions-=m

" kill the scrollbars
  set guioptions-=r
  set guioptions-=L

" use a big, pretty font
  set guifont=Monaco:h15
